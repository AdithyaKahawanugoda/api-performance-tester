# MongoDB
MONGO_PASSWORD=changeme_dev
MONGODB_URI=mongodb://admin:changeme_dev@localhost:27017/api-perf?authSource=admin

# Redis
REDIS_PASSWORD=changeme_dev
REDIS_URL=redis://:changeme_dev@localhost:6379

# Backend
PORT=4000
NODE_ENV=development
LOG_LEVEL=debug

# Worker
WORKER_CONCURRENCY=50

# Frontend (Next.js public env vars — prefix with NEXT_PUBLIC_)
NEXT_PUBLIC_API_URL=http://localhost:4000/api
NEXT_PUBLIC_WS_URL=ws://localhost:4000/ws
